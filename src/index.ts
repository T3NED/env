import { envString } from "./resolvers/string";
import { envNumber } from "./resolvers/number";
import { envInteger } from "./resolvers/integer";
import { envBoolean } from "./resolvers/boolean";

export {
	envString,
	envString as string,
	envString as str,
	envNumber,
	envNumber as number,
	envNumber as num,
	envInteger,
	envInteger as integer,
	envInteger as int,
	envBoolean,
	envBoolean as boolean,
	envBoolean as bool,
};
